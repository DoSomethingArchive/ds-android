<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res/org.dosomething.android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/campaign_share_outer_background"
    android:orientation="vertical" >

    <com.markupartist.android.widget.ActionBar
        android:id="@+id/actionbar"
        style="@style/ActionBar"
        app:title="@string/sign_up_title" />
	
    <ScrollView 
		android:orientation="vertical"
		android:layout_weight="1.0"
		android:layout_width="fill_parent"
		android:layout_height="0dp"
		android:fillViewport="true">
		
        <LinearLayout 
            android:orientation="vertical"
            android:layout_width="fill_parent"
			android:layout_height="wrap_content"
			style="@style/WebFormRow">
			
            <LinearLayout
	            android:id="@+id/web_form"
	            android:orientation="vertical"
	            android:layout_width="fill_parent"
				android:layout_height="wrap_content"/>
            
            <TextView android:id="@+id/required_instructions" style="@style/RegisterDisclaimer" android:text="@string/register_disclaimer" android:visibility="gone"/>
            
            <Button
		        android:id="@+id/submit"
		        style="@style/WebFormRowSubmit"
		        android:text="@string/go_upper" />
        </LinearLayout>
        
    </ScrollView>
    
</LinearLayout>